{
  "name": "openarena-pk3-parser",
  "version": "0.1.0",
  "description": "This library can be used to inspect OpenArena PK3 files and the contained BSP files",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:w": "tsc -w",
    "exec:bsp": "tsc && node dist/test-bsp-parser.js",
    "exec:pk3": "tsc && node dist/test-pk3-parser.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Leib",
  "repository": {
    "type": "git",
    "url": "git@github.com:mucer/openarena-pk3-parser.git"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/fs-extra": "^5.0.4",
    "@types/node": "^10.12.7",
    "@types/unzipper": "^0.9.1",
    "rimraf": "^2.6.2",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "fs-extra": "^7.0.1",
    "unzipper": "^0.9.4"
  }
}
